export class AppSettings {
  // Toastr Options
   public static TOASTR = {
      position: ["top", "right"],
      timeOut: 2000,
      lastOnBottom: true,
      showProgressBar: false
    };

  public static HEADER_HEIGHT = 50;
  public static MENU_WIDTH = 60;
}